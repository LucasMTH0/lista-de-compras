export interface Product {
  id: number;
  name: string;
  url: string;
  image: string;
  price: string;
  userId: string;
  listId: string,
  priority: string,
  category: string,
  status: boolean,
  createdAt: Date,
}
